Важно! Важно! Важно! Очень Важно! Твоя задача составить профессиональный промт из моих заметок и набросков ниже. Не пиши код, не пиши реализацию. Составь профессиональный промт программного архитектора, который учитывает все нюаныс, тонкости, узкие места.


# Green Kiss

Домен: greenkiss.ru

## Цель: разработать интернет-магазин.

Инструменты: next js 16.0.3

## Задачи:

1) Создать архитектуру данных: схемы данных, взаимосвязи. Выстроить правильную `O` сложность CRUD операций, поиска, сортировки, фильтрации.

Инструменты: Postgres js и Drizzle ORM.

2) Определить уровни доступа на клиенте, сервере, валидацию, типы ответов и ошибок.

3) Написать серверные экшены и маршруты АПИ для CRUD, фильтрации, сортировки, поиска. На освное разработанных связий данных, `O` сложности, валидации, уровня доступа и так далее.

4) Разработать интерфес админ панели.

5) Разработать интерфейс и логику интернет магазина. Применяя лучшие практики оптимизации.

## UX

1) Логика и взгляд пользователя:

- Зайдя на сайт я вижу красивый, понятный хедер:
Лого с ссылкой на главную страницу; Кнопкой меню с надписью "Каталог", при нажатии открывает каталог; Поиск по сайту; Иконка темы; Профиль иконка, открывает поповер; Иконка доставки; иконка фаворит листа в виде сердца; иконка корзинки.

- Ниже хедера красивый баннер, который автоматически красиво слайдером меняется на другой, но можно руками вернуть на предыдущий. 

> Он грузиться быстро. Так как первый загружается сразу, А остальные лениво после загрузки страницы, так можно сделать? Чтобы интерфес грузился первым. Наверное даже лучше все картинки грузить с S3.

- Ниже банеров идут карточки товаров.

Карточка имеет вид:

Картинка, на которой в правом верхнем углу иконка сердца для добавления в фаворит лист. Верхний левый угол для леймблов типа: SALE, NEW и так далее. Нижний левый кгол для размера скидки типа "-61%" и сезонных акицй типа "ЧЕРНАЯ ПЯТНИЦА", при наведении на карточку, внизу исчезает скидка и сезонная акция, но на всю ширину появляется кнопка "Быстрый просмотр". 

Цена и если со скидкой то правее предыдущая цена зачеркнутая. Сам вид цены тоже есть нескольких видов. primary цвета, red цвета если супер цена.

Название бренда

Название товара

Иконка звездочки и среднняя оценка, иконка комментария и количество оценок

Кнопка на всю ширину с иконкой корзины и надписью сегодня.

2) При нажатии на товар

Вертикальная карусель картинок, далее главная картинка, далее блок описания товара и выбор цвета и так далее, далее цена с кнопками купить сейчас, корзина.

Ниже описание и характеристики

Ниже отзывы, вопросы.

3) При выборе категори вид как на главном экране, но без банеров и слева на десктопе панель с фильтрацией, сортирвкой. Адаптируй под разные размеры экрана.

При оплате выбирается адрес и время доставки. Можно заказать без оплаты примерку на дом на конкретное время.

Используй лучшие практики. Категории могут быть любой вложености. 

Я разрабатываю интернет-магазин на drizzle, next js, postgres. Твоя задача сначала разработать архитекруту схема, что с чем связанно, нарисовав схематично.

Вот что я хочу:

Схема Категории:

- Любой уровень вложености
- Название на русском
- Название для строки поиска (генерируется автоматически специальной функцией заменяя руские символы английскими. Например Женщинам --> zhenshchinam)
- Картинка (на странице категории показывается)
- Все поля для Опен Граф соц сетей (список напиши, автоматически используют ссылку на картикну, но для соц сетей можно другую выбрать)

Что еще я упустил важного? Мне кажется описание не нужно, или нужно?

Схема брендов:

- Название (на любом языке)
- КатегорииID[] (Бренд должен быть привязан к категориям верно? Но не к одной, а может к множеству. Но лучше сразу делать так чтобы был быстрый поиск. Также стоит учесть, что при создании товара и выборе для товара категорий, они автоматически должны добавиться за О от 1 к бренду, при этом уникально. Также если удаляется товар или категория, то очень правильно сложность О должны проверятся все бренды и у них чиститься категории если нужно в зависимостях). И если добавляется категория то и все ее родительские категории верно? А если удаляется категоряи или товар, то чиститься не только категория в зависимостях, но и все ее родительские, верно (только здесь важно проверить, например есть: Женщинам, Брюки, Легинцы. Вот Легенцы удаляются если единственный товар с этим брендом и этими ленгицами удалился,А Брюки надо проверить, может есть другая дочерний товар этого бренада в категории Брюки, верно?)?. 
- Картинка (думаю здесь 32x32px подойдет? Нужно будет автоматически резать и сжимать и менять на webp в админке) (опциональна)
- ТоварID: Бренд должен быть у любого тровар

Всякие SEO штуки нужны ли для бренда? Типа картинка, описание и так далее? Я думаю что нет. Это и так геморойно заполнять. Достаточно картинки и названия. верно? 

Что еще я упустил?

Схема Фильтра или Харектиристик или еще как-то:

- Например цвет, или сезон, или размер и так далее.
- Привязывается к конкретной категории и всем ее родительским категориям верно? Также должна быстро отвязыватсья при удалении категоррий и быстро привязываться. Тут важно учесть, что если у нас есть Женщинам, Брюки, Легинцы. И характеристика материал привязывается к Легинцы. А легинцы удаляются, то нужно ли оставлять эту характиристику у Брюки?
- ТоварID - если у товаров это категории есть характеристика, то у любого твоара этйо категории обязательно должна быть характеристика
- Нужны ли картинки по эту категория (напрмиер если выбрано у цвета то теперь при смене цвета меняются и картинки). Важно ЧТо картинок можно до 5 штук загрузить, также если только есть картинка, то это полноценный товар. Например Если у цветов есть картинки, а у размеров нет. То Пользователь увидет на странице только green куртку и red куртку, а не green l куртка, red  m куртка, red xs курта и так далее. А если картинки не выбраны, даже если разные цвте, то все ранво будет один товар показан. Это тоже учти, чтобы был быстрый поиск по базе данных.

Всякие сео штуки не нужны, отделньой страницы под каждую зхарактеристику не будет

Что еще я упустил?

Схема цены:

- Есть у всех товаров
- ТоварID
- Скидка (придумай как сделать, чтобы было и в процентах можно было сделать и в размере (типо 10 рублей), может еще какой вариант? Может каждое поле отдельно? Или отдельно просто вынести тип скидки. Скидка не обязательна).

Схема лейблов:

- Это типа когда на карточки товара и описании указаны лейблы: SALE и так далее. Здесь нужно сделать общую схему и ты предложи правило для каждого лейбла (Типо SALE действует от таково процента). Еще лучше есмли можно их вешать, а также они могут вешать автоматически, а также их можно запретить, что бы не вкешались автоматически. Подумай как лучше сделать предложи вараинты.

Схема Товара:

- ТоварID
- КатегорияID[]
- ЛейбылID[]
- Название
- Картинка

И так далее.

Что еще я упустиЛ, но супер важно?

Также количсевто товаров, товары фавориты (сердечко поставил). 

Все сехмы нужно писать с мысслю о супер быстром поиске (напрмиер поиск по названию бренад это когда в фильрах в секции брендов в инпуте вводянт название и так для каждой характеристики). А также о супер быстрйо сортирвоке и фильтрации чтобы моментально отправлять на клиент. Ведь загрузка будет по. Также у каждого товара (тех характирист что есть картинки) должно быть поле с мини картинкой (это супер легкая версия главной картинки чтобы быстро грузился огромный список товаров).

Что еще я упустил? 

ТАкже нужно учитывать у каждого клиента что он купил. Что он отложил в фаварит товары, что в корзине, что в доставке и так далее. 

ВОт текущая схема что есть

import type { AdapterAccountType } from "@auth/core/adapters";
import {
  boolean,
  integer,
  pgEnum,
  pgTable,
  primaryKey,
  text,
  timestamp,
} from "drizzle-orm/pg-core";
import { createId } from "@/shared/lib/id";

export const userRoleEnum = pgEnum("user_role", ["USER", "ADMIN", "MANAGER"]);

export const users = pgTable("user", {
  id: text("id")
    .primaryKey()
    .$defaultFn(() => createId()),
  name: text("name"),
  email: text("email").unique(),
  emailVerified: timestamp("emailVerified", { mode: "date" }),
  image: text("image"),
  role: userRoleEnum("role").notNull().default("USER"),
  isBanned: boolean("isBanned").notNull().default(false),
});

export const accounts = pgTable(
  "account",
  {
    userId: text("userId")
      .notNull()
      .references(() => users.id, { onDelete: "cascade" }),
    type: text("type").$type<AdapterAccountType>().notNull(),
    provider: text("provider").notNull(),
    providerAccountId: text("providerAccountId").notNull(),
    refresh_token: text("refresh_token"),
    access_token: text("access_token"),
    expires_at: integer("expires_at"),
    token_type: text("token_type"),
    scope: text("scope"),
    id_token: text("id_token"),
    session_state: text("session_state"),
  },
  (account) => [
    {
      compoundKey: primaryKey({
        columns: [account.provider, account.providerAccountId],
      }),
    },
  ],
);

export const sessions = pgTable("session", {
  sessionToken: text("sessionToken").primaryKey(),
  userId: text("userId")
    .notNull()
    .references(() => users.id, { onDelete: "cascade" }),
  expires: timestamp("expires", { mode: "date" }).notNull(),
});

export const verificationTokens = pgTable(
  "verificationToken",
  {
    identifier: text("identifier").notNull(),
    token: text("token").notNull(),
    expires: timestamp("expires", { mode: "date" }).notNull(),
  },
  (verificationToken) => [
    {
      compositePk: primaryKey({
        columns: [verificationToken.identifier, verificationToken.token],
      }),
    },
  ],
);

export const authenticators = pgTable(
  "authenticator",
  {
    credentialID: text("credentialID").notNull().unique(),
    userId: text("userId")
      .notNull()
      .references(() => users.id, { onDelete: "cascade" }),
    providerAccountId: text("providerAccountId").notNull(),
    credentialPublicKey: text("credentialPublicKey").notNull(),
    counter: integer("counter").notNull(),
    credentialDeviceType: text("credentialDeviceType").notNull(),
    credentialBackedUp: boolean("credentialBackedUp").notNull(),
    transports: text("transports"),
  },
  (authenticator) => [
    {
      compositePK: primaryKey({
        columns: [authenticator.userId, authenticator.credentialID],
      }),
    },
  ],
);


Напиши рьщуюу архзитекруту объясни плюсы и минусы. Что не учтено, а что учтено. Дальше мы согласуем по этапно каждый кусок архитекрутуы и придем к единой, затем напишем схемы drizzel


И вот такую схему мне написал ИИ

Category (категория)

id: уникальный идентификатор
name: имя на русском (например, «Женщинам». Пока не будем делать перевод на несколько языков одного твоара. Таково даже на амазон нет)
description: описание категории, нужно ограмничить количсевто символов на клиенет и сервер (придумай нормальное)
slug: слаг для URL (автогенерация из русского: «Женщинам → zhenshchinam», уникален внутри одного родителя)
full_path: полный путь для роутинга (например, women/leggings). Делает открытие страницы по URL мгновенным
parent_id: ссылка на родителя (null для корня)
image_id: основная картинка (страница категории, админка будет автоматически сжимать и переводить в webp, сама база будет хранить ссылку на S3)
seo-поля (минимально необходимое):
meta_title (по умолчанию = name)
meta_description (Обязательно)
og_title (по умолчанию = name)
og_description (Обязательно)
og_type (обычно website)
canonical_url (собирается автоматически из домена + full_path)
meta_robots (например, index,follow (выбери правильный))
status: draft | active | hidden (удобно скрывать разделы без удаления)
sort_order: число для ручной сортировки в меню (это зачем нужно?)
level: глубина в дереве (0 для корня) — удобно и быстро для интерфейса
created_at / updated_at

- Вот  как я скоректирвоал. Также давай так сделаем! Картинку всем категориям сделаем из базовой картинки сайта. То есть есть какая-то отдельная таблица состоящая из картинки сайта (а может там и лого и фавиконку разместим и будем качать не с сервера а с S3? Тогда вообще удобно будет, как считваешь?) Чтобы все категории имели в соц сетях картинку логотипа сайта. А вот товары уже будут иметь свою картинку для соц сетей. Поменяй это и напиши итоговую схему.

---

CategoryClosure (карта родства)
ancestor_id: предок
descendant_id: потомок
depth: расстояние (0 — это категория сама себе предок)
Ограничение уникальности: (ancestor_id, descendant_id)
Индексы: по ancestor_id и по descendant_id (для быстрых запросов «вверх» и «вниз» по дереву)
Что это дает на практике:

Быстрая выборка всех предков для хлебных крошек.
Быстрая выборка всех потомков (включая вложенные уровни) — важно для фильтрации товаров по целой ветке.
Быстрый пересчет «агрегатов» вверх по дереву (количество товаров/брендов и т. д.).

---

MediaAsset (медиафайлы)
id
original_url (линк на оригинал - давай уберем, зачем оригинал хранить и память забивать? Или нужен? Просто сразу его сжимаем и в webp и вот его уже можно и резать и сжимать и оптимизировать, верно?)
width/height, mime, bytes (это зачем?)
alt (альтернативный текст для SEO/доступности)
dominant_color (доминирующий цвет для плейсхолдеров - это зачем? Не нужно, у всего сайта будет стандартизированный скелетон)
lqip (очень легкое превью — base64 или data-URL - это зачем?)
variants (json): заранее сгенерированные размеры и webp (например, 32×32, 256×256, 1024×…, webp) - Вот здесь мне кажется нужно заменить на два поля (webp и сжатый для превью, как считаешь?)
created_at

