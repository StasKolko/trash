{
  "name": "@scripts/prompt-bundler",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "check:type": "tsc --noEmit",

    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:run": "vitest run"
  },
  "dependencies": {
    "clipboardy": "5.3.1",
    "tinyglobby": "0.2.15"
  },
  "devDependencies": {
    "@configs/typescript": "workspace:*",
    "@configs/vitest": "workspace:*",
    "@vitest/coverage-v8": "catalog:test",
    "vitest": "catalog:test"
  }
}
