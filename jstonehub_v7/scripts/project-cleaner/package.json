{
  "name": "@scripts/project-cleaner",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",

    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@scripts/util": "workspace:*",
    "picomatch": "4.0.3"
  },
  "devDependencies": {
    "@configs/typescript": "workspace:*",
    "@configs/vitest": "workspace:*",
    "vitest": "catalog:test",
    "@vitest/coverage-v8": "catalog:test",
    "@types/picomatch": "4.0.2"
  }
}
