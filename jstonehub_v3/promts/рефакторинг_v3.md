## Текущие задачи

1) Напиши тест пейдж для всех вариантов Popover (импорт из @packages/ui/overlays). ВАжно чтобы они были в разных состояниях. А также сделай возможность менять прпосы (абсолютно все) чтобы првоерить реактивность каждого. Все состояния обязательно выводи рядом для наглядности

import { createFileRoute } from "@tanstack/solid-router";

const Route = createFileRoute("/test")({
  component: RouteComponent,
});

function RouteComponent() {
  return <div>Hello "/test"!</div>;
}

export { Route };

---

## Ошибки

---

## Код